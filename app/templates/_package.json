{
  "name": "asimov-<% if (contrib) { %>contrib-<% } %><%= componentName %>",
  "version": "0.0.0",
  "description": "<%= description %>",
  "keywords": [
    "asimov"
  ],
  "readmeFilename": "README.md",
  "dependencies": {},
  "devDependencies": {
    "grunt": "~0.4.2"
  },
  "engines": {
    "node": ">=0.8.0"
  },
  "license": "MIT",
  "main": "Gruntfile.js",
  "scripts": {
    "setup": "bower update; (cd bower_components/asimov-core; npm install);",
    "build": "npm run setup && grunt",
    "start": "npmrun setup && grunt dev"
  },
}
